#pragma once

// DataManagementSetClass.cpp includes this production header but does not use
// PlotWidget in its implementation.  Keep the unrelated plot/UI graph outside
// this DataManagement component test.
